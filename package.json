{
  "name": "kratz",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun scripts/build.ts",
    "dev": "bun --hot scripts/serve.ts",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/bun": "^1.0.4",
    "prettier": "^3.2.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
